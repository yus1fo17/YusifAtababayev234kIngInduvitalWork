# Spring Boot application name
spring.application.name=MealPass

spring.datasource.url=jdbc:postgresql://localhost:5432/mealpassdbv1
spring.datasource.username=bidlogiquser
spring.datasource.password=StrongPassw0rd!
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server port
server.port=8080

# JWT properties
mealpass.jwt.secret=verySecretJwtKeyChangeMe
mealpass.jwt.expiration-ms=3600000
